[project]
name = "a2"
version = "0.1.1"
requires-python = ">=3.13"
dependencies = [
    "l2",
]

[dependency-groups]
dev = [
    "pyright>0",
    "pytest>0",
]

[project.scripts]
a2 = "a2:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
