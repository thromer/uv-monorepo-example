[project]
name = "l2"
version = "0.1.1"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "pytest>0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
