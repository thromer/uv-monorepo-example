[project]
name = "l2"
version = "0.1.1"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.4",
]

[dependency-groups]
dev = [
    "pyright>0",
    "pytest>0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
