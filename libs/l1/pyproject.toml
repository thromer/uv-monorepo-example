[project]
name = "l1"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "cowsay>=6.1",
]

[dependency-groups]
dev = [
    "pyright>0",
    "pytest>0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
